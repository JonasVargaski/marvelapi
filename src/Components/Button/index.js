import styled from "styled-components";

export const Button = styled.button`
  font-size: 1rem;
  padding: 8px 14px;
  border: none;
  border-radius: 4px;
  &:hover {
    filter: brightness(0.8);
  }
`;
